# Local glove assets

Scanned glove meshes, textures, fitted MANO references, transferred rigs, and
generated model sequences are local experiment assets and are excluded from
Git. Restore them separately if running the glove-retargeting scripts.

They are not required by the current hand-restoration training configuration.
